services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: tablewise-backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  db:
    image: postgres:16-alpine
    container_name: tablewise-db
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tablewise_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tablewise_password}
      POSTGRES_DB: ${POSTGRES_DB:-tablewise_db}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        - "CMD-SHELL"
        - "pg_isready -U ${POSTGRES_USER:-tablewise_user} -d ${POSTGRES_DB:-tablewise_db}"
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: tablewise-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  postgres_data:
  redis_data: